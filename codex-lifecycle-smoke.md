# Codex Git lifecycle smoke proof

This file proves the registered task-worktree lifecycle can commit, push, open a pull request, wait for the required CI gate, merge into `codex/integration`, and remove its clean worktree automatically.
